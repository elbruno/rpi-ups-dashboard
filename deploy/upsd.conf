# /etc/nut/upsd.conf — NUT daemon network configuration
# Listen only on localhost — the Flask app runs on the same Pi.
# Do NOT expose 3493 to the network unless you add authentication.

LISTEN 127.0.0.1 3493
