# /etc/nut/nut.conf — NUT operating mode
# standalone = this Pi manages the UPS directly and monitors it locally.
# Use "netserver" if other machines need to query this UPS over the network.

MODE=standalone
